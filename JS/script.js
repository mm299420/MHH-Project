
var opened = true;

function AdminRootTools() {
    rootTools = document.getElementById("AdminTools")
    if (rootTools.style.display == "none") {
        rootTools.style.display = "inline-block";
    } else {
        rootTools.style.display = "none";
    }
}
